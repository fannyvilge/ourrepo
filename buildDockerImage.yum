
- hosts: localhost
  tasks:
  - name: install docker-py
    pip: name=docker-py version=1.9.0
    become: yes
    become_method: sudo
  - name: create image
    docker_image: path=~/myDocker/ name=myimage state=present
